export enum LS {
  LS_TOKEN_SYSTEM = 'token-map-zero-web',
  LS_LANGUAGE = 'language-map-zero-web',
}

export enum LimitValues {
  TEN = 10,
}

export enum FormSubmitActions {
  SAVE = 'save',
  SEND = 'send', // send email
  MARK_AS_SENT = 'mark_as_sent',
}
